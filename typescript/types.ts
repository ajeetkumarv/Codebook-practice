let a: number;
let b: boolean;
let c: string
let d: any
let e: number[] = [1,2,3]
let f: any[] = [1, true, 'a'];

const ColorRed = 0;
const ColorBlue = 1;

enum Color {Red, blue}; // internally {Red = 0, blue = 1}

console.log('Color is: ' + Color.Red); // output: Color i: 0


